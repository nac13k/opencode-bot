{
  "name": "opencode-telegram-bridge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "start": "node dist/main.js",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "format:check": "node -e \"console.log('No formatter configured yet')\"",
    "setup": "tsx setup/installer.ts",
    "test": "node --test"
  },
  "dependencies": {
    "@opencode-ai/plugin": "^0.5.15",
    "dotenv": "^16.4.7",
    "grammy": "^1.35.0"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "tsx": "^4.19.2",
    "typescript": "^5.9.2"
  }
}
